useEffect - dependency array,
    useEffect(() => {

    }, [])
useState() => store state of the component

advantages to create component

Props to child components(pass data from parent to child components)

destructure -> const { name } = { name: 'shruti' };

... (spread, rest operator)

api calling - axios

array methods
    push
    pop
    shift
    unshift
    map
    filter
    slice
    splice
    reduce
    foreach
    forLoop
    length

localStorage


setTimeOut(() => {
    // function excecute when time out
}, 10000)
clearTimeOut - stop Timeout
setInterval(() => {
    // funtion trigger on every interval
}, 100)
clearInterval - stop interval

server start
sms

git complete operations


